/*define([
	'underscore',
	'backbone',
	'models/LicenseModel'
], function(_, Backbone, LicenseModel) {

	return Backbone.Collection.extend({

		model: LicenseModel,
	  
	  url: function() {
	  	return "api/licenses";
	  },

	});

});
*/