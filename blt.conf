# Blt Configuration File

# Log file and level
blt.log.file = CONSOLE
blt.log.count = 10
blt.log.level = WARN

#blt.log.class.aaa.User.java = ALL
#blt.log.class.snmp.SnmpPollingTask.java = DEBUG
blt.log.class.topology.TopologyService.java = WARN

#in some circumstances, for example ISIS L1/L2 or OSPF redistribution, same prefix can be originated by multiple routers
#In order to avoid duplicate snmp target, we do not consider prefixes which metric is higher than "localIgpMaxMetric" to be directly connected. 
#blt.router.localIgpMaxMetric = 10

blt.snmp.timeout = 3000
blt.snmp.retries = 3
blt.snmp.taskthrottletime = 10000

blt.dump.filename = blt-data.xml